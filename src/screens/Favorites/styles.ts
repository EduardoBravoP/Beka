import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 20,
    backgroundColor: '#F8F8F8',
    height: '100%'
  },
  listColumn: {
    gap: 16,
    marginBottom: 16
  },
  listContainer: {
    marginTop: 8,
  }
});